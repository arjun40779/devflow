# Tooling

Shared monorepo tooling consumed by apps and packages.

| Package | Purpose |
| --- | --- |
| `@devflow/eslint-config` | ESLint flat configs (`base`, `next`) |
| `@devflow/tsconfig` | TypeScript `extends` presets |

Root-level Prettier config lives at `../.prettierrc.json`.
